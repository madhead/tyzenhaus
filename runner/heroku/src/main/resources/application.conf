ktor {
  deployment {
    port = 5000
    port = ${?PORT}
  }

  application {
    modules = [
      me.madhead.tyzenhaus.runner.heroku.TyzenhausKt.main
    ]
  }
}

telegram {
  token = ${TELEGRAM_TOKEN}
}

database {
  url = ${DATABASE_URL}
}
